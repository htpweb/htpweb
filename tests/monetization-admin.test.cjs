const test=require("node:test");const assert=require("node:assert/strict");const fs=require("node:fs");const path=require("node:path");const h=fs.readFileSync(path.join(__dirname,"../admin/monetizacion.html"),"utf8"),j=fs.readFileSync(path.join(__dirname,"../admin/monetization.js"),"utf8");test("ADMIN gestiona planes y prestaciones",()=>{assert.match(j,/master_save_subscription_plan/);assert.match(h,/Prestaciones/)});test("ADMIN asigna y consulta consumo",()=>{assert.match(j,/master_assign_subscription_plan/);assert.match(j,/plan_usage_snapshot/)});test("MASTER gestiona excepciones",()=>{assert.match(j,/master_set_plan_override/);assert.match(h,/Excepción MASTER/)});test("roles no MASTER solo visualizan scope permitido",()=>{assert.match(j,/current_role_code/);assert.match(j,/master-only/)});